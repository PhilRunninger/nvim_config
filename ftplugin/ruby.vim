" Tab settings
set softtabstop=2   " number of spaces that <tab> uses when editing
set tabstop=2       " number of spaces that <tab> in file uses
set shiftwidth=2    " number of spaces to use for (auto)indent step
set expandtab       " use spaces when <tab> is inserted

" fix the SLOOOW syntax highlighting
set regexpengine=1      " This selects the default regexp engine. (old engine)
set foldmethod=manual   " The kind of folding used for the current window.
