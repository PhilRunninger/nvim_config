" Tab settings
setlocal softtabstop=2   " number of spaces that <tab> uses when editing
setlocal tabstop=2       " number of spaces that <tab> in file uses
setlocal shiftwidth=2    " number of spaces to use for (auto)indent step
setlocal expandtab       " use spaces when <tab> is inserted

" fix the SLOOOW syntax highlighting
setlocal regexpengine=1      " This selects the default regexp engine. (old engine)
setlocal foldmethod=manual   " The kind of folding used for the current window.
